@startuml
!include https://raw.githubusercontent.com/plantuml-stdlib/C4-PlantUML/master/C4_Container.puml

LAYOUT_LANDSCAPE()

title Diagrama de Containers

Person(visitante, "Visitante", "Usuário não autenticado que consome a plataforma")
Person(aluno, "Aluno", "Aluno que pode participar dos projetos")
Person(professor, "Professor", "Professor que pode criar dos projetos")
Person(comunidade, "Comunidade Externa", "usuário autenticado como comunidade externa que interage melhor na plataforma")


System_Boundary(system, "TADSXANDRIA BIBLIOTECA") {
  Container(front, "Frontend", "Angula", "Mostra os templates do sistema")
  Container(back, "Backend", "Django Rest", "Processa os dados dos sistema e faz conexões via API")
  ContainerDb(database, "Banco de Dados", "Postgres", "Armazena dados do sistema")
}

System_Ext(suap, "SUAP", "Auxilia a vinculação dos dados com o sistema, através da matrícula do SUAP")
System_Ext(github, "GitHub", "Auxilia a vinculação dos dados com o sistema, através da conta GitHub")
System_Ext(google, "Google", "Auxilia a vinculação dos dados com o sistema, através da conta Google")

Rel(visitante, front, "Visita a plataforma para conhecê-la")
Rel(aluno, front, "Edita os projetos que participa, sendo como orientado")
Rel(professor, front, "Gerencia os projetos que o cadastrou, sendo como orientador")
Rel(comunidade, front, "Interage nos projetos cadastrados")


Rel(back, suap, "Faz a conexão com o sistema externo")
Rel(back, github, "Faz a conexão com o sistema externo")
Rel(back, google, "Faz a conexão com o sistema externo")

Rel(front, back, "Chama a requisição http")
Rel(back, database, "Acessa informações do sistema")

@enduml
